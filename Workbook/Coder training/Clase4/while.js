for (let i= 1 ; i <= 5; i = i + 1) {

    let puntos = parseInt (prompt ("Ingrese puntos obtenidos 0 - 1 - 3 partido " + i));

    if (puntos = 3 || 1 || 0) {
        console.log ("El equipo obtuvo; " + puntos);
    }

}


function sumar_puntos (a,b) {
    
    for (let i= 1 ; i <= 5; i = i + 1) {

        let puntos = parseInt (prompt ("Ingrese puntos obtenidos 0 - 1 - 3 partido " + i));

        if (puntos = 3 || 1 || 0) {
            console.log ("El equipo obtuvo; " + puntos);
        }
}
}